# Application name
spring.application.name=register-login

# Database Configuration (Using Railway-provided environment variables)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# File upload settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Default Spring Security (use your own credentials or remove for production)
spring.security.user.name=${ADMIN_USERNAME:vighnesh}
spring.security.user.password=${ADMIN_PASSWORD:v123}


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=webappfarmer@gmail.com
spring.mail.password=gycx zlja btlp huke
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
